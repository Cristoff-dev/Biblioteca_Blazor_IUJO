@page "/"

<div class="home-wrapper">
	<div class="bienvenida-container">
		<div class="bienvenida-overlay">
			<h1 class="titulo-principal">Bienvenido al Sistema de Biblioteca IUJO Barquisimeto</h1>

			<p class="descripcion">
				Este sistema ha sido desarrollado para la gestión eficiente de libros, estudiantes y préstamos
				de la biblioteca del <strong>Instituto Universitario Jesús Obrero</strong>.
			</p>

			<p class="descripcion">
				Use el menú de navegación lateral para acceder a los módulos de
				<span class="modulo">Libros</span>,
				<span class="modulo">Estudiantes</span>,
				<span class="modulo">Préstamos</span> y
				<span class="modulo">Devoluciones</span>.
			</p>
		</div>
	</div>
</div>

<style>
	/* 🔒 Asegura que el contenedor principal ocupe todo el espacio */
	.home-wrapper {
		width: 100%;
		height: 100%;
		display: flex;
		flex-grow: 1;
	}

	.bienvenida-container {
		background-image: url("/img/iujo-barquisimeto.jpg");
		background-size: cover;
		background-position: center;
		background-repeat: no-repeat;
		width: 100%;
		min-height: 100vh;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.bienvenida-overlay {
		max-width: 900px;
		text-align: center;
		color: #fff;
		padding: 2rem;
		background-color: rgba(0, 0, 0, 0.5);
		border-radius: 12px;
		box-shadow: 0 0 20px rgba(0,0,0,0.4);
	}

	.titulo-principal {
		font-size: 2.8rem;
		font-weight: 700;
		margin-bottom: 1.5rem;
		text-shadow: 2px 2px 6px rgba(0,0,0,0.6);
	}

	.descripcion {
		font-size: 1.2rem;
		margin-bottom: 1rem;
		line-height: 1.6;
	}

	.modulo {
		font-weight: 600;
		color: #ffeb3b;
	}

	/* 🔒 Elimina márgenes globales que causan bordes blancos */
	html, body {
		margin: 0;
		padding: 0;
		height: 100%;
		background-color: transparent;
		font-family: "Segoe UI", "Helvetica Neue", Arial, sans-serif;
	}
</style>
